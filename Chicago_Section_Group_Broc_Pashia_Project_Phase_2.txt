Broc Pashia CS 425 Phase 2create table customer	(email			varchar(50)  primary key,	 firstName		varchar(20),	 middleName		varchar(20),	 lastName		varchar(20),	 homeAirport		varchar(3),	 foreign key (homeAirport) references airport (locIdentifier)	);create index email_index on customer(email)create table airport	(locIdentifier		varchar(3) primary key, 	 name			varchar(50), 	 country		varchar(30),	 state			varchar(20)	);create index loc_identifier_index on airport(locIdentifier)create table airline	(airlineCode		varchar(2) primary key, 	 name			varchar(50), 	 country		varchar(30),	);create index airline_index on airport(airlineCode)create table booking	(bookingID			varchar(10) primary key,	 flightClass			varchar(10),	 email				varchar(50),	 creditCardNumber		int,	 foreign key (email) references customer (email),	 foreign key (creditCardNumber) references creditCard (creditCardNumber)	);create index booking_index on airport(bookingID)	 create table address	(addressID			varchar(10),	 email				varchar(50),	 streetAddress			varchar(100),	 city				varchar(50),	 state				varchar(20),	 zip				int,	 primary key (addressID, email)	 foreign key (email) references customer (email)	);create table creditCard	(email				varchar(50),	 addressID			varchar(10),         creditCardNumber		int, 	 expirationDate			date,	 securityCode			int,	 )	 primary key (email, creditCardNumber),       foreign key (email) references customer (email),      foreign key (addressID) references address (addressID)			);create table flight	(airlineCode			varchar(2),	 flightNumber			int,	 flightDate			date,	 firstClassCapacity,		int,	 economyClassCapacity		int,	 arrivalTime			time,	 departureTime			time,	 departureAirport		varchar(3),	 destinationAirport		varchar(3),	 primary key (airlineCode, flightNumber, flightDate),	 foreign key (airlineCode) references airline (airlineCode)foreign key (departureAirport, destinationAirport) references airport (locIdentifier, locIdentifier)	);create table price	(airlineCode			varchar(2),	 flightNumber			int,	 flightDate			date,	 firstClassPrice		float,	 economyClassPrice		float	 primary key (airlineCode, flightNumber, flightDate),	 foreign key (airlineCode) references airline (airlineCode)       foreign key (flightNumber, flightDate) references flight (flightNumber, flightDate)	);create table bookingFlight	(bookingID			varchar(10),	 airlineCode			varchar(2),	 flightNumber			int,	 flightDate			date,	 primary key( bookingID, airlineCode, flightNumber, flightDate),	 foreign key ( bookingID) references booking (bookingID)	 foreign key (airlineCode) references airline (airlineCode)       foreign key (flightNumber, flightDate) references flight (flightNumber, flightDate)      )